name: Check build ready
on:
  pull_request_target:
    types:
      - opened
      - synchronize
    branches:
      - main
    paths:
      - "src/**"
jobs:
  build:
    runs-on: ubuntu-latest
    permissions: write-all
    steps:
      - uses: actions/checkout@v3
      # Setup .npmrc file to publish to GitHub Packages
      - name: Set commit status as pending
        uses: ouzi-dev/commit-status-updater@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          status: pending
          name: Build Status
      - uses: actions/setup-node@v3
        with:
          node-version: 18
          # Defaults to the user or organization that owns the workflow file
      - run: npm ci
      # set new version
      - name: Build
        run: npm run build
      - name: check if build is ready
        if: ${{ success() }}
        uses: ouzi-dev/commit-status-updater@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          status: success
          name: Build Status
      - name: ouzi-dev/commit-status-updater@v2
        if: ${{ failure() }}
        uses: ouzi-dev/commit-status-updater@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          status: failure
          name: Build Status
